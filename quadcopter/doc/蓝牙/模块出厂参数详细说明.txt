1.V5xx版本
  出厂为透传模式;波特率9600，N，8，1;不自动休眠;串口无连接状态输出提示;本地串口AT指令不支持多种输入模式，只接受无回车换行符的指令。
2.V6xx版本;只支持一个绑定设备记忆。
  出厂为透传+远控模式;波特率9600，N，8，1;不自动休眠;串口有连接状态输出提示;支持三个绑定设备记忆;本地串口AT指令支待多种输入模式，V603开始支持回车换行和大小写混入的AT指令，V605开始支持所求即所得的指令方式，当用户发送带回车换行的指令的时候，模块会回复带回车换行的指令，用户发送的指令如果不带回车换行，那么模块回复的也将不带。并且支持大小写混入。